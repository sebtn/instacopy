<div class="box-in-posts col-xs-10 col-xs-offset-1">
	<h1>Post something cool here</h1>
	<%= simple_form_for @post do |f| %>
		<%= f.input :title %>
		<%= f.input :description %>
		<br />
		<%= f.submit 'Create', :class => 'btn btn-primary' %>
	<% end %>	
</div>	