<div class="box-in-posts col-xs-10 col-xs-offset-1">
	<h1>Upload an image</h1>
	<%= simple_form_for @photo :url => post_comments_path(@post) do |f| %>
		<%= f.input :picture %>	
		<br />
		<%= f.submit 'Create', :class => 'btn btn-success' %>
	<% end %>	
</div>	